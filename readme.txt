# Crypto Alpha AI Advisor — CLI-сервис вероятностного анализа крипторынка

Полное описание архитектуры, логики работы, схемы базы данных, структуры файлов и инструкции по развертыванию. Весь проект умещается в этот один файл — копируйте и создавайте свою систему.

---

## 📌 1. КОНЦЕПЦИЯ

Автоматический аналитический сервис, который:

1. **Собирает данные** с DexPaprika (бесплатно, без API-ключа): цены, объемы, количество покупок/продаж, ликвидность по активным токенам.
2. **Обогащает** новостным фоном через CryptoPanic.
3. **Отправляет структурированный слепок рынка** в DeepSeek API (ваш ИИ, доступный через OpenAI-совместимый эндпоинт), где на основе исторических паттернов 2019–2026 годов формируется вероятностный прогноз.
4. **Получает JSON-ответ** с фазами рынка, конкретными сигналами (актив, диапазон входа, стоп-лосс, тейк-профит, вероятность успеха, исторический аналог).
5. **Фильтрует сигналы** по уровню уверенности (>65%) и соотношению риск/прибыль (>1.5).
6. **Отправляет** отформатированные сигналы в Telegram.
7. **Сохраняет всё** в PostgreSQL (TimescaleDB) для последующего бэктестинга и улучшения модели.

Цель: не предсказывать будущее, а выдавать **вероятностные оценки** на основе объективных исторических повторений.

---

## 🧱 2. АРХИТЕКТУРА СИСТЕМЫ
[Источники данных] → [Сборщики] → [PostgreSQL/TimescaleDB] → [AI-анализатор] → [Фильтр] → [Telegram]
↑ ↑ ↑ ↑
DexPaprika Планировщик Исторические DeepSeek API
DexScreener (Celery Beat) паттерны (ваш ИИ)
CryptoPanic

text

**Компоненты:**

| Компонент | Назначение | Технологии |
|-----------|-----------|------------|
| **Сборщики** | Периодический опрос API | Python, requests, asyncio |
| **Хранилище** | Временные ряды + метаданные | PostgreSQL + TimescaleDB |
| **Очередь задач** | Планирование сбора/анализа | Celery + Redis |
| **AI-ядро** | Генерация вероятностных сигналов | DeepSeek API (OpenAI-совместимый эндпоинт) |
| **Фильтр** | Отсев низкокачественных сигналов | Python (pandas) |
| **Telegram-бот** | Доставка сигналов пользователю | python-telegram-bot |
| **CLI** | Управление и ручной запуск | Click |

---

## 🗃️ 3. СХЕМА БАЗЫ ДАННЫХ (PostgreSQL + TimescaleDB)

```sql
-- Таблица 1: Метаданные токенов
CREATE TABLE token_metadata (
    id SERIAL PRIMARY KEY,
    network VARCHAR(50) NOT NULL,          -- ethereum, bsc, solana, polygon
    token_address VARCHAR(100) NOT NULL,
    symbol VARCHAR(50),
    name VARCHAR(255),
    first_seen TIMESTAMP DEFAULT NOW(),
    last_updated TIMESTAMP DEFAULT NOW(),
    UNIQUE(network, token_address)
);

-- Таблица 2: Ценовые снимки (гипертаблица TimescaleDB)
CREATE TABLE price_snapshots (
    time TIMESTAMPTZ NOT NULL,
    token_id INTEGER REFERENCES token_metadata(id),
    price_usd DECIMAL(20, 8),
    liquidity_usd DECIMAL(20, 2),
    volume_24h DECIMAL(20, 2),
    fdv_usd DECIMAL(20, 2),               -- Fully Diluted Valuation
    market_cap_usd DECIMAL(20, 2)
);
SELECT create_hypertable('price_snapshots', 'time');

-- Таблица 3: Торговая активность (КЛЮЧЕВОЙ ИСТОЧНИК СИГНАЛОВ)
CREATE TABLE trade_activity (
    time TIMESTAMPTZ NOT NULL,
    token_id INTEGER REFERENCES token_metadata(id),
    buys_1h INTEGER,                      -- количество покупок за час
    sells_1h INTEGER,                    -- количество продаж за час
    buys_24h INTEGER,
    sells_24h INTEGER,
    txns_1h INTEGER,                     -- всего транзакций за час
    txns_24h INTEGER,
    volume_1h DECIMAL(20, 2)
);
SELECT create_hypertable('trade_activity', 'time');

-- Таблица 4: Новостной фон
CREATE TABLE news_articles (
    id SERIAL PRIMARY KEY,
    published_at TIMESTAMPTZ,
    title TEXT,
    source VARCHAR(100),
    sentiment FLOAT,                     -- -1.0 .. 1.0 (оценка тональности)
    url TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Таблица 5: AI-сигналы (то, что генерирует DeepSeek)
CREATE TABLE ai_signals (
    id SERIAL PRIMARY KEY,
    generated_at TIMESTAMP DEFAULT NOW(),
    asset VARCHAR(50),                   -- BTC, ETH, RENDER, WIF и т.д.
    action VARCHAR(20),                 -- BUY_ON_DIP, SELL, HOLD
    entry_min DECIMAL(20, 8),
    entry_max DECIMAL(20, 8),
    stop_loss DECIMAL(20, 8),
    take_profit DECIMAL(20, 8),
    probability DECIMAL(5, 2),          -- вероятность успеха (0-100)
    confidence DECIMAL(5, 2),           -- уверенность модели (0-100)
    risk_reward DECIMAL(5, 2),          -- соотношение риск/прибыль
    reasoning TEXT,                    -- краткое обоснование
    historical_analogs TEXT,           -- какой период повторяется
    sent_to_telegram BOOLEAN DEFAULT FALSE
);

-- Таблица 6: Бэктестинг (оценка точности сигналов)
CREATE TABLE signal_performance (
    signal_id INTEGER REFERENCES ai_signals(id),
    actual_price_after_48h DECIMAL(20, 8),
    hit_take_profit BOOLEAN,
    hit_stop_loss BOOLEAN,
    pnl_percent DECIMAL(10, 2),
    evaluated_at TIMESTAMP DEFAULT NOW()
);
Почему TimescaleDB?
Вы будете хранить миллионы строк ценовых снэпшотов. TimescaleDB автоматически партиционирует данные по времени, даёт ускорение в 20-100x для запросов типа SELECT * FROM price_snapshots WHERE time > now() - interval '7 days'.

📁 4. СТРУКТУРА ФАЙЛОВ ПРОЕКТА (ПОЛНАЯ)
text
crypto-alpha-ai/
│
├── docker-compose.yml               # Подъем PostgreSQL + Redis
├── requirements.txt                 # Python-зависимости
├── .env.example                     # Шаблон переменных окружения
├── .env                             # Создать локально (не в git)
│
├── config/
│   └── settings.py                 # Все настройки из .env
│
├── database/
│   ├── __init__.py
│   ├── models.py                  # SQLAlchemy ORM-модели
│   └── db_manager.py              # Подключение, инициализация, CRUD
│
├── collectors/
│   ├── __init__.py
│   ├── dex_paprika.py            # ОСНОВНОЙ сборщик (цены, сделки, ликвидность)
│   ├── dex_screener.py           # Дополнительный (новые пары, мемы)
│   └── news_collector.py         # CryptoPanic / RSS
│
├── analyzers/
│   ├── __init__.py
│   ├── ai_adapter.py            # Отправка данных в DeepSeek API, парсинг JSON
│   └── signal_generator.py      # Локальная фильтрация сигналов
│
├── telegram/
│   ├── __init__.py
│   └── bot.py                   # Форматирование и отправка сигналов
│
├── scheduler/
│   ├── __init__.py
│   └── tasks.py                # Celery-задачи (сбор, анализ, отправка)
│
├── cli/
│   ├── __init__.py
│   └── main.py                 # Click-команды (ручное управление)
│
└── run.py                       # Точка входа для продакшена
⚙️ 5. ЛОГИКА РАБОТЫ — ПОШАГОВО
🔄 Шаг 1. Сбор данных (Collectors)
DexPaprikaCollector — сердце системы.

python
# Псевдокод реальной логики
def collect_for_analysis(network="ethereum", limit=30):
    # 1. Получаем список активных токенов (сортировка по объему 24h)
    tokens = get_trending_tokens(network, limit)
    
    # 2. По каждому токену забираем детальные метрики:
    for token in tokens:
        data = {
            'symbol': ...,
            'price_usd': ...,
            'liquidity_usd': ...,
            'volume_24h': ...,
            'buys_1h': ...,    # ← УНИКАЛЬНОЕ ПРЕИМУЩЕСТВО DEXPAPRIKA
            'sells_1h': ...,   # ← ЭТОГО НЕТ В БОЛЬШИНСТВЕ API
            'buys_24h': ...,
            'sells_24h': ...,
            'txns_1h': ...,
            'txns_24h': ...
        }
    return collected_data
Почему это важно для вероятностного анализа?
Соотношение buys_1h / sells_1h — лучший опережающий индикатор разворота. Когда количество покупок резко превышает количество продаж, но цена ещё не выросла — это зона входа.

NewsCollector — забирает топ-10 новостей с CryptoPanic, вычисляет грубый сентимент (на основе голосов).

🧠 Шаг 2. AI-анализ (AIAdapter)
Формируется JSON-слепок рынка:

json
{
  "timestamp": "2026-02-11T14:30:00Z",
  "market_metrics": {
    "btc_dominance": 52.4,
    "fear_greed": 72,
    "total_market_cap": 2.8e12
  },
  "top_tokens": [ ... 20 токенов с полной ончейн-статистикой ],
  "news_summary": "BlackRock обновляет заявку на ETH-ETF...",
  "request": "Дай 3 сигнала с вероятностью >65% и конкретными ценами"
}
Этот JSON отправляется в DeepSeek API (ваш ИИ) с системным промптом, где модель выступает в роли вероятностного аналитика, знающего исторические паттерны 2019–2026.

Пример ответа AI:

json
{
  "market_phase": "Ранний альтсезон",
  "signals": [
    {
      "asset": "ETH",
      "action": "BUY_ON_DIP",
      "entry_min": 3450.0,
      "entry_max": 3550.0,
      "stop_loss": 3300.0,
      "take_profit": 4100.0,
      "probability": 74.0,
      "confidence": 82.0,
      "risk_reward": 2.3,
      "historical_analog": "Январь 2024, за 3 недели до ETF",
      "reasoning": "Киты накапливают ETH второй день подряд, L2-активность на максимумах за 3 месяца, MVRV в зоне недооцененности"
    }
  ]
}
Критически важно: Модель никогда не предлагает покупать на хаях. Только на откатах с чётким уровнем входа.

🧹 Шаг 3. Фильтрация и приоритезация
Не каждый сигнал достоин Telegram. Правила фильтрации (задаются в settings.py):

MIN_SIGNAL_CONFIDENCE = 0.65 — минимум 65% уверенности.

MIN_RISK_REWARD = 1.5 — соотношение риск/прибыль не ниже 1.5.

MAX_SIGNALS_PER_DAY = 10 — лимит, чтобы не спамить.

INCLUDE_MEMECOINS = true — опционально.

Также система не повторяет один и тот же актив, пока сигнал не устареет (48 часов).

📤 Шаг 4. Отправка в Telegram
Формат сообщения:

text
🎯 СИГНАЛ [RENDER]

Действие: ПОКУПКА НА ОТКАТЕ
Вход: $8.20–8.60
Стоп-лосс: $7.50 (-9.3%)
Тейк-профит: $11.00 (+28.0%)

🔥 Вероятность успеха: 74.0%
⚡ Уверенность: 78.0%
📊 Risk/Reward: 2.4

📚 Исторический аналог: Февраль 2024, AI-сезон
💡 Обоснование: Объемы RNDR/FET растут третьи сутки, 
      доминирование AI-нарратива возвращается, 
      технически паттерн «бычий флаг» на 4H.

⏳ Актуален: 48 часов
💾 Шаг 5. Сохранение и бэктестинг
Каждый сигнал сохраняется в ai_signals. Через 48 часов автоматический скрипт:

Берёт финальную цену актива.

Проверяет, был ли достигнут тейк-профит или стоп-лосс.

Записывает результат в signal_performance.

Это создаёт вашу собственную базу знаний: через 3-4 месяца вы точно знаете, в каких фазах рынка сигналы работают лучше всего, а в каких — хуже.

🐳 6. ПОЛНЫЙ КОНФИГУРАЦИОННЫЙ ФАЙЛ (docker-compose.yml)
yaml
version: '3.8'

services:
  postgres:
    image: timescale/timescaledb:latest-pg15
    container_name: crypto_ai_db
    environment:
      POSTGRES_USER: crypto
      POSTGRES_PASSWORD: crypto_pass
      POSTGRES_DB: crypto_alpha
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crypto"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: crypto_redis
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - redis_data:/data

  worker:
    build: .
    container_name: crypto_worker
    command: celery -A scheduler.tasks worker --loglevel=info
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped

  beat:
    build: .
    container_name: crypto_beat
    command: celery -A scheduler.tasks beat --loglevel=info
    depends_on:
      - redis
      - worker
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
📦 7. REQUIREMENTS.TXT (точный список)
text
psycopg2-binary==2.9.9
sqlalchemy==2.0.23
alembic==1.13.1
timescale==0.1.0
requests==2.31.0
celery==5.3.4
redis==5.0.1
python-telegram-bot==20.7
openai==1.6.1
pandas==2.1.4
numpy==1.26.2
python-dotenv==1.0.0
schedule==1.2.0
click==8.1.7
🔐 8. ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ (.env.example)
env
# ----- БАЗА ДАННЫХ -----
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crypto_alpha
DB_USER=crypto
DB_PASS=crypto_pass

# ----- API КЛЮЧИ -----
DEEPSEEK_API_KEY=sk-...                    # Ваш ключ DeepSeek
DEEPSEEK_API_BASE=https://api.deepseek.com/v1  # Или ваш кастомный эндпоинт
DEEPSEEK_MODEL=deepseek-chat              # Модель по умолчанию

TELEGRAM_BOT_TOKEN=...                  # Создать у @BotFather
TELEGRAM_CHAT_ID=...                   # Узнать у @userinfobot

# ----- ОПЦИОНАЛЬНО -----
CRYPTOPANIC_API_KEY=...               # Бесплатно на cryptopanic.com

# ----- НАСТРОЙКИ АНАЛИЗА -----
COLLECTION_INTERVAL_MINUTES=30        # Частота сбора данных
MIN_SIGNAL_CONFIDENCE=0.65           # Минимальная уверенность (65%)
MAX_SIGNALS_PER_DAY=10              # Лимит алертов
INCLUDE_MEMECOINS=true              # Учитывать ли мемкойны
MIN_RISK_REWARD=1.5                # Минимальное соотношение риск/прибыль
🤖 9. АДАПТЕР ДЛЯ DEEPSEEK API (ВМЕСТО OPENAI)
Вместо OpenAI используем DeepSeek с полной совместимостью:

python
# analyzers/ai_adapter.py
import json
import requests
from config.settings import Config

class DeepSeekAnalyzer:
    """Адаптер для отправки данных в DeepSeek API"""
    
    def __init__(self):
        self.api_key = Config.DEEPSEEK_API_KEY
        self.api_base = Config.DEEPSEEK_API_BASE
        self.model = Config.DEEPSEEK_MODEL
        
    def analyze_market_data(self, market_data, news_summary):
        """
        Отправляет собранные данные в DeepSeek и получает сигналы
        """
        
        system_prompt = """Ты — вероятностный крипто-аналитик с доступом к историческим паттернам 2019-2026.
        
Твоя задача: на основе переданных ончейн-данных, торговой активности и новостей:
1. Определить текущую фазу рынка (ранний бычий, поздний бычий, медвежий, консолидация)
2. Выделить топ-3 наиболее перспективных актива для входа на ОТКАТЕ (не на хаях!)
3. По каждому активу дать:
   - Конкретную цену входа (диапазон)
   - Стоп-лосс (в $ и %)
   - Тейк-профит (в $ и %)
   - Вероятность успеха (0-100%)
   - Уверенность в сигнале (0-100%)
   - Соотношение риск/прибыль
   - Исторический аналог (конкретный период)
   - Краткое обоснование

ВАЖНО: Ты НЕ советуешь покупать прямо сейчас. Ты указываешь цену ОТКАТА.
Формат ответа — строго JSON."""
        
        user_prompt = f"""
=== РЫНОЧНЫЕ ДАННЫЕ ===
{json.dumps(market_data[:20], indent=2, default=str)[:3000]}... (обрезано)

=== НОВОСТНОЙ ФОН ===
{news_summary}

=== ИНСТРУКЦИЯ ===
Верни JSON в формате:
{{
  "market_phase": "бычий/медвежий/консолидация",
  "signals": [
    {{
      "asset": "BTC",
      "action": "BUY_ON_DIP",
      "entry_min": 60000.0,
      "entry_max": 61000.0,
      "stop_loss": 58000.0,
      "take_profit": 67000.0,
      "probability": 75.0,
      "confidence": 80.0,
      "risk_reward": 2.1,
      "historical_analog": "Январь 2024, перед ETF",
      "reasoning": "Краткое обоснование"
    }}
  ]
}}
"""
        
        try:
            response = requests.post(
                f"{self.api_base}/chat/completions",
                headers={
                    "Authorization": f"Bearer {self.api_key}",
                    "Content-Type": "application/json"
                },
                json={
                    "model": self.model,
                    "messages": [
                        {"role": "system", "content": system_prompt},
                        {"role": "user", "content": user_prompt}
                    ],
                    "temperature": 0.3,
                    "response_format": {"type": "json_object"}
                },
                timeout=30
            )
            
            response.raise_for_status()
            result = response.json()
            content = result["choices"][0]["message"]["content"]
            return json.loads(content)
            
        except Exception as e:
            print(f"❌ Ошибка DeepSeek API: {e}")
            # Возвращаем заглушку при ошибке
            return {
                "market_phase": "не определено",
                "signals": []
            }
    
    def analyze_with_mock(self, market_data, news_summary):
        """Заглушка для тестирования без API-ключа"""
        print("⚙️ Используется mock-анализ (без DeepSeek)")
        
        return {
            "market_phase": "ранний альтсезон",
            "signals": [
                {
                    "asset": "ETH",
                    "action": "BUY_ON_DIP",
                    "entry_min": 3450.0,
                    "entry_max": 3550.0,
                    "stop_loss": 3300.0,
                    "take_profit": 4000.0,
                    "probability": 72.0,
                    "confidence": 78.0,
                    "risk_reward": 2.0,
                    "historical_analog": "Март 2024, предхайп ETF",
                    "reasoning": "ETH обновляет ATH на фоне ETF-ожиданий, L2-активность растет, киты накапливают"
                },
                {
                    "asset": "RENDER",
                    "action": "BUY_ON_DIP",
                    "entry_min": 8.2,
                    "entry_max": 8.6,
                    "stop_loss": 7.5,
                    "take_profit": 11.0,
                    "probability": 68.0,
                    "confidence": 65.0,
                    "risk_reward": 2.4,
                    "historical_analog": "Февраль 2024, AI-сезон",
                    "reasoning": "AI-нарратив возвращается, объемы растут, сильная поддержка"
                }
            ]
        }
🚀 10. ИНСТРУКЦИЯ ПО ЗАПУСКУ (5 минут до первого сигнала)
Шаг 1. Подготовка окружения
bash
# 1. Клонируйте репозиторий (или создайте папку вручную)
mkdir crypto-alpha-ai && cd crypto-alpha-ai

# 2. Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Создайте .env файл
cp .env.example .env
nano .env  # Вставьте свои ключи
Шаг 2. Запуск базы данных
bash
# Через Docker Compose
docker-compose up -d postgres redis

# Проверка, что БД готова
docker-compose ps
Шаг 3. Инициализация схемы БД
bash
python -c "from database.db_manager import DatabaseManager; DatabaseManager().init_db()"
Шаг 4. Тестовый сбор данных
bash
# Проверяем, что DexPaprika отвечает
python cli/main.py collect --network ethereum --limit 5
Должны увидеть: 📊 Собрано 5 токенов с ethereum

Шаг 5. Тестовый AI-анализ (без DeepSeek)
bash
python cli/main.py analyze --mock
Если всё работает — вы увидите сигналы-заглушки.

Шаг 6. Полный цикл (реальный DeepSeek)
bash
# Убедитесь, что в .env указан DEEPSEEK_API_KEY
python cli/main.py full-cycle
Через 10-20 секунд в ваш Telegram придёт первый сигнал.